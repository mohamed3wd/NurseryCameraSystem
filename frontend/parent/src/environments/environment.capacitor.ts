/**
 * Production mobile build (Capacitor).
 * Point apiUrl/hubUrl at your deployed API before releasing to the stores.
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.your-nursery.example/api',
  hubUrl: 'https://api.your-nursery.example/hubs/nursery'
};
